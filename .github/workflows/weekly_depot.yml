name: 📈 Weekly Depot Review

on:
  schedule:
    # Jeden Samstag um 07:00 UTC (= 08:00 MEZ)
    - cron: "0 7 * * 6"
  workflow_dispatch:
    # Manueller Trigger zum Testen
    inputs:
      reason:
        description: 'Grund für manuellen Start'
        required: false
        default: 'Manuelles Portfolio Update'

jobs:
  analyze_depot:
    runs-on: ubuntu-latest
    
    steps:
      - name: "🔄 Repository auschecken"
        uses: actions/checkout@v4
        
      - name: "🐍 Python Setup"
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          
      - name: "📦 Abhängigkeiten installieren"
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          
      - name: "📊 Depot analysieren"
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GOOGLE_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          EMAIL_ADDRESS: ${{ secrets.EMAIL_ADDRESS }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          EMAIL_RECIPIENT: ${{ secrets.EMAIL_RECIPIENT }}
        run: |
          python depot_analyzer.py
